package application.model.search.cfind

import application.model.rules.hrot.HROT
import application.model.rules.isotropic.rules.INTGenerations
import org.junit.jupiter.api.Test

import org.junit.jupiter.api.Assertions.*

class ShipSearchTest {
    /*@Test
    fun search() {
        var shipSearchParameters = ShipSearchParameters(HROT("B267/S035"), 8, 1, 3,
            symmetry = Symmetry.EVEN_SYMMETRIC)
        ShipSearch(shipSearchParameters).search(1)

        shipSearchParameters = ShipSearchParameters(INTGenerations("0/2/3"), 8, 1, 2)
        ShipSearch(shipSearchParameters).search(1)

        shipSearchParameters = ShipSearchParameters(HROT("B3567/S3568"), 6, 1, 4,
            symmetry = Symmetry.ODD_SYMMETRIC)
        ShipSearch(shipSearchParameters).search(1)
    }*/
}